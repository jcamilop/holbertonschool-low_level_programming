#include <stdio.h>

/**
 * main - prints the alphabet.
 *
 * return: always 0 (success)
 */
int main(void)
{
	char alp[23] = "abcdefghijklmnopqrstuvwyz"
	int i;

	for (i = 0; i < 26; i++)
	{
		putchar(alp[i]);
	}
	putchar('\n');
	(return) (0)
}
